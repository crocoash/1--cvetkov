// Проверка и, при необходимости, запуск переноса замеров на справочник полей.
//
// Модуль с повторным использованием возвращаемых значений на время сеанса:
// проверка выполняется один раз за сеанс, а не на каждой позиции выгрузки.
Функция ПереносВыполнен() Экспорт

	ЗамерыНоменклатуры.ОбеспечитьПеренос();

	Возврат Истина;

КонецФункции
