# Задание: опубликовать HTTP-сервис ТелеграмВебхук и включить вебхук

Это задание для Claude Code, запущенного **на машине с 1С и веб-сервером**. Здесь, на машине
разработки, ни базы, ни Конфигуратора, ни веб-сервера нет — поэтому всё, что ниже, проверить
отсюда невозможно, и написано оно по коду, а не по наблюдению.

Цель: нажатия кнопок под сообщениями менеджеру в Telegram должны приходить в 1С **мгновенно**,
через вебхук, а не опросом раз в минуту.

## Что уже сделано (трогать не нужно)

* В конфигурации есть `HTTPСервис.ТелеграмВебхук` — `Корневой URL = tg`, шаблон `/hook`,
  метод `Принять` (POST). Модуль: `HTTPServices/ТелеграмВебхук/Ext/Module.bsl`.
* Есть роль `ТелеграмВебхукДоступ` с единственным правом — вызвать этот сервис. Больше ей ничего
  не нужно: код внутри первым делом включает привилегированный режим.
* В `CommonModules/ТелеграмБот/Ext/Module.bsl` есть функция `СекретВебхука()` — сейчас возвращает
  пустую строку. **Пустой секрет = сервис отвечает 403 на всё**, вебхук выключен, нажатия
  забирает регламент `ТелеграмОбмен` опросом `getUpdates`.
* Токен бота — в функции `ТокенБота()` того же модуля. Бот `@cvetkov_manager_bot`.

## Факты с машины (разведка 07.08.2026, C-RDS01SRV)

Задание писалось вслепую; ниже — то, что реально обнаружено на месте. Где факты расходятся
с шагами ниже, **верны факты**, а шаги отмечены поправками.

| | |
|---|---|
| Хост | `C-RDS01SRV`, домен `CV.local`, 10.200.200.5 — **терминальный сервер**, не сервер 1С |
| ОС | Windows Server 2025 Standard, `ProductType = 3` → ветка `Install-WindowsFeature` |
| Платформа | 8.3.25.1374, x64 и x86 — совпадает с `Configuration.xml` |
| База | **клиент-серверная**: `Srvr="c-db01srv";Ref="work"` (10.200.200.4). Здесь только клиент и Конфигуратор, `WEBSERVEREXT=0` |
| IIS | установлен, `W3SVC` Running. `Web-CGI` **доустановлен 07.08.2026** |
| Модули расш. веб-сервера 1С | **нет** — `wsisapi.dll` отсутствует в обоих `bin` |
| Публикации 1С | нет ни одной, `default.vrd` на диске не найден |
| Дистрибутив | **нет на диске**: `InstallSource` = `C:\1\` и `C:\33\`, `C:\33` удалён |
| Внешний адрес | исходящий NAT `5.255.168.246`; проброс внутрь не подтверждён |
| DNS домена | `cvetkovtm.com` обслуживается `inhostedns.*` (не Cloudflare), `tg.cvetkovtm.com` сейчас резолвится в хостинг сайта `185.68.16.67` |
| Сертификаты | только самоподписанные RDP `CN=C-RDS01SRV.CV.local`, оба просрочены |

**МИКО здесь нет.** Ни публикации на этой машине, ни веб-сервера на `c-db01srv` — там открыты
только 1540/1541, портов 80/443 нет вообще. Пиггибэкнуться не на что, публикация делается с нуля.

### Поправка 1: порт 8443 и отдельный сайт IIS, а не 443

На этом IIS уже работает **RD Web Access** (`/RDWeb`, `/RDWeb/Pages`, `/RDWeb/Feed`,
`/RDWeb/FeedLogin`), и Default Web Site забинден на `*:80` и `*:443`. Пробросить 443 наружу —
значит выставить в интернет форму входа в терминальный сервер. Так делать нельзя.

Вместо этого: **отдельный сайт IIS `tg1c` на порту 8443** (Telegram принимает 443, 80, 88, **8443**),
свой пул приложений, свой сертификат. RDWeb остаётся на 443 и наружу не пробрасывается вообще.
Наружу пробрасывается только 8443 → 10.200.200.5:8443.

Плюс правило Windows Firewall: вход на 8443 разрешён только с подсетей Telegram
`149.154.160.0/20` и `91.108.4.0/22`.

### Поправка 2: самоподписанный сертификат, а не Let's Encrypt

Задание ниже советует «проще нормальный сертификат». На месте это оказалось **не проще**:

* HTTP-01 валидация Let's Encrypt требует открытого наружу порта 80 — а на 80 висит тот же
  Default Web Site с RDWeb;
* DNS-01 требует API у `inhostedns`, доступа к нему нет;
* любой вариант ACME — это продление раз в 60 дней на машине, где это некому чинить.

Telegram штатно поддерживает самоподписанный сертификат: публичная часть загружается в `setWebhook`
параметром `certificate`, и дальше Telegram сверяет именно её. Это убирает и порт 80, и продления.
Сертификат выпускается на 10 лет, `CN=tg.cvetkovtm.com`, SAN содержит и имя, и IP.

Минус ровно один: браузер на такой адрес ругается. Для machine-to-machine эндпоинта это неважно,
`curl` из шага 5 запускать с `-k`.

### Что осталось сделать и кем

| шаг | кто | состояние |
|---|---|---|
| `Web-CGI` | сделано 07.08.2026 | ✅ |
| сайт `tg1c`:8443 + сертификат + firewall | `setup-tgsite.ps1` от администратора | ✅ сделано 07.08.2026 |
| ~~дистрибутив 8.3.25.1374~~ | не понадобился | ✅ снят, см. ниже |
| модули расширения веб-сервера | перенесены с `c-db01srv` | ✅ сделано 07.08.2026 |
| публикация `/tgbot` + `default.vrd` | `publish-tgbot.ps1` от администратора | ✅ сделано 07.08.2026 |
| загрузка конфигурации, пользователь `ТелеграмВебхук` | пользователь, Конфигуратор | ✅ сделано 07.08.2026 |
| `<httpServices>` и `Usr`/`Pwd` в `default.vrd` | `vrd-httpservices.ps1` | ✅ сделано 07.08.2026 |
| A-запись `tg.cvetkovtm.com` → 5.255.168.246 | пользователь, панель хостинга | ✅ сделано 07.08.2026 |
| проброс 8443 на роутере → 10.200.200.5 | пользователь, MikroTik | ✅ сделано 07.08.2026 |
| секрет + `setWebhook` | сделано 07.08.2026 | ✅ **вебхук работает** |

### Вебхук включён 07.08.2026

```
url ....................  https://tg.cvetkovtm.com:8443/tgbot/hs/tg/hook
has_custom_certificate .  true
ip_address .............  5.255.168.246
pending_update_count ...  0
last_error_message .....  отсутствует
```

Кнопка в чате отрабатывает мгновенно. Опрос `getUpdates` при этом выключен заполненным
секретом, регламент `ТелеграмОбмен` продолжает развозить очередь уведомлений — так и задумано.

**Где что лежит на машине с 1С:**

| | |
|---|---|
| репозиторий | `C:\Users\anton\Documents\fork\1--cvetkov` |
| скрипты публикации и логи | `C:\ProgramData\tgsetup\` (это НЕ репозиторий, `git pull` там не работает) |
| публикация | `C:\inetpub\tg1c\tgbot\default.vrd` |
| роутер | MikroTik L009UiGS, `10.200.200.253`, вход только Winbox (8291); правило NAT №12 |

**Сертификат.** Файла `telegram-webhook.crt` на месте не оказалось — `setup-tgsite.ps1` кладёт его
рядом с собой, а запускали скрипт из другого каталога. Пересоздаётся из хранилища по отпечатку
одной строкой, ключ для этого не нужен — в `setWebhook` уходит только публичная часть:

```powershell
$c = Get-Item Cert:\LocalMachine\My\63CC26542562410C4B3ABE6D887726B1D929AE60; $pem = "-----BEGIN CERTIFICATE-----`r`n" + [Convert]::ToBase64String($c.RawData,'InsertLineBreaks') + "`r`n-----END CERTIFICATE-----`r`n"; [IO.File]::WriteAllText('C:\ProgramData\tgsetup\telegram-webhook.crt',$pem,(New-Object Text.ASCIIEncoding))
```

**Ловушка проверки снаружи.** Правило Windows Firewall пускает только подсети Telegram, поэтому
проверка с любого другого адреса даёт таймаут при полностью исправной публикации — это выглядит
как «проброс не работает» и стоило нам часа. На время проверки правило расширяется до `Any`,
потом сужается обратно:

```powershell
Set-NetFirewallRule -DisplayName 'Telegram webhook 1C (8443)' -RemoteAddress Any
Set-NetFirewallRule -DisplayName 'Telegram webhook 1C (8443)' -RemoteAddress @('149.154.160.0/20','91.108.4.0/22')
```

Результат обязательно перечитывать — первый вызов молча не применился:

```powershell
Get-NetFirewallRule -DisplayName 'Telegram webhook 1C (8443)' | Get-NetFirewallAddressFilter | Select-Object RemoteAddress
```

### Сайт в это правило не вписан — «вернуть в чат» из админки не работает (09.08.2026)

Симптом: в админке `↩ вернуть в чат` даёт «1С не вернула карточку: нет связи с 1С:
Connection timed out after 10001 milliseconds». Это ровно та же ловушка, что абзацем выше,
только теперь стучится не проверяющий, а **сервер сайта** — а он в белом списке не значится.

Замерено с машины разработки и с самого сайта 09.08.2026:

| проверка | результат | вывод |
|---|---|---|
| `dig tg.cvetkovtm.com` | `5.255.168.246` | DNS в порядке, WAN2 Maxnet активен |
| `ping 5.255.168.246` | 0% потерь, 21 мс | роутер жив, переключения на Triolan не было |
| `nc -z 5.255.168.246 8443` | таймаут | порт фильтруется по адресу источника |
| исходящий порт 8443 с сайта (`portquiz.net:8443`) | **открыт** | хостинг исходящие не режет |
| **исходящий IP сайта** | **`185.104.44.136`** | его и нет в правиле |

Адрес сайта пришлось измерять: `$_SERVER['SERVER_ADDR']` на этом хостинге отдаёт `127.0.0.1`
(PHP за прокси), а `185.68.16.67` из A-записи `cvetkovtm.com` — это входящий адрес, исходящий
NAT у хостера **другой**. Проверка добавлена в `shop/debug_1c_log.php?ping1c=1` — она печатает
и исходящий IP, и доступность порта 8443 наружу.

Лечение на машине с 1С, от администратора (правило одно, адреса перечисляются списком):

```powershell
Set-NetFirewallRule -DisplayName 'Telegram webhook 1C (8443)' `
  -RemoteAddress @('149.154.160.0/20','91.108.4.0/22','185.104.44.136')

Get-NetFirewallRule -DisplayName 'Telegram webhook 1C (8443)' |
  Get-NetFirewallAddressFilter | Select-Object RemoteAddress
```

Второй вызов обязателен: выше уже отмечено, что `Set-NetFirewallRule` умеет молча не примениться.

Если после этого таймаут остался — ограничение не на Windows, а на **MikroTik**: смотреть
правило NAT №12 (`10.200.200.253`, Winbox), нет ли у него `src-address-list` с подсетями Telegram.
Проверять после правки не с рабочей машины, а с сайта:

```
https://cvetkovtm.com/assets/shop/debug_1c_log.php?token=<ТОКЕН>&ping1c=1
```

Ожидаемый ответ — `ok = 1` и состояние регламента `ТелеграмОбмен`, а не таймаут.

**Оставшийся риск: два провайдера.** WAN2 Maxnet `5.255.168.246` (активный, на него смотрит
A-запись и правило NAT), WAN1 Triolan `178.151.80.183` в резерве. При переключении канала вебхук
отвалится: Telegram будет стучаться на мёртвый адрес. Лечится второй парой — правило NAT на адрес
Triolan плюс замена A-записи на CNAME → `hgn09t26xam.sn.mynetname.net` (MikroTik IP Cloud держит
это имя в актуальном состоянии сам). Не сделано намеренно: сначала должен был заработать
основной путь.

### Публикация принята (07.08.2026)

`POST https://localhost:8443/tgbot/hs/tg/hook` → **403**, то есть сервис вызвался и отклонил запрос
из-за пустого секрета. Это и был критерий готовности.

Ловушка при приёмке: тело ответа — не пустое, а страница IIS «Подробные данные об ошибке
403.0». 1С отдаёт 403 **с пустым телом**, и IIS подменяет его своей страницей (для запросов
с localhost — подробной). Отличить своё 403 от чужого можно по полям этой страницы:

```
Модуль       IsapiModule
Уведомление  ExecuteRequestHandler
Обработчик   1C Web-service Extension
Код ошибки   0x00000000
```

`IsapiModule` + `1C Web-service Extension` = запрос прошёл через 1С. Было бы
`StaticFileModule`/`DirectoryListingModule` — значит завернул сам IIS.

### Дистрибутив не понадобился

`releases.1c.ru` для этой конфигурации недоступен (украинская поставка, ИТС нет), но и не нужен:
на **`c-db01srv`** стоит сервер 1С той же версии 8.3.25.1374 **с модулями расширения веб-сервера**.
Оттуда скопированы два файла в `bin` терминального сервера:

```
C:\Program Files\1cv8\8.3.25.1374\bin\wsisapi.dll   (257 952, x64, 8.3.25.1374)
C:\Program Files\1cv8\8.3.25.1374\bin\webinst.exe   (108 448, 8.3.25.1374)
```

Дистрибутив, из которого ставили сервер (`D:\1\srv\`), уже удалён — копирование файлов оказалось
единственным путём и сработало. Компонент при этом не прописан в реестре и `1CEStart.cfg`;
на работу вебхука это не влияет, всплывёт только при следующем обновлении платформы.

### Публикация (07.08.2026)

```
приложение /tgbot в сайте tg1c, пул tg1c, каталог C:\inetpub\tg1c\tgbot
isapiCgiRestriction: wsisapi.dll разрешён
handlers: overrideMode=Allow точечно для сайта tg1c
POST https://localhost:8443/tgbot/hs/tg/hook -> 401
RDWeb -> 302 (не задет)
```

**401 — правильный промежуточный ответ**: `Usr`/`Pwd` в `default.vrd` не прописаны, потому что
пользователя `ТелеграмВебхук` в базе ещё нет. Но 401 доказывает главное — ISAPI-модуль загрузился,
IIS его пустил, запрос дошёл до 1С.

### Ловушка: webinst не публикует HTTP-сервисы

Сгенерированный `webinst` файл содержит только `<ws pointEnableCommon="true"/>`,
`<standardOdata enable="false"/>` и `<analytics enable="true"/>`. **Элемента `<httpServices>`
в нём нет вообще**, то есть HTTP-сервис не опубликован и по адресу `/hs/tg/hook` будет 404.

Дописывает его `vrd-httpservices.ps1`. Точную схему элемента проверить было негде: строк
`httpServices` / `pointEnableCommon` нет ни в `wsisapi.dll`, ни в 244 других файлах `bin` —
разбор `vrd` идёт на стороне сервера. Поэтому скрипт пишет **и** общий флаг, **и** явную запись
о сервисе, чтобы сработал хоть один вариант:

```xml
<httpServices pointEnableCommon="true">
	<service name="ТелеграмВебхук" rootUrl="tg" enable="true"/>
</httpServices>
```

Проверить это можно только после загрузки конфигурации в базу: пока `HTTPСервис.ТелеграмВебхук`
в базе не существует, «сервис не опубликован» и «сервиса нет в конфигурации» дают одинаковый 404.

Скрипт также снимает у `default.vrd` наследование прав. Причина: пароль в `vrd` лежит открытым
текстом, а машина терминальная — по умолчанию файл прочитал бы любой пользователь сервера.

**Правки скрипта с машины разработки (07.08.2026), перед запуском сделать `git pull`:**

* повторный запуск (смена пароля) ломал `vrd`. Шаблон удаления старого блока был один —
  `(?s)\s*<httpServices.*?(?:/>|</httpServices>)`, и ленивый `.*?` останавливался на `/>`
  вложенного `<service .../>`, оставляя в файле висячий `</httpServices>`. Воспроизведено локально:
  после второго прохода `vrd` перестаёт быть валидным XML. Теперь два отдельных шаблона —
  парный блок и самозакрывающийся тег;
* `BUILTIN\Администраторы` в списке прав заменён на SID `S-1-5-32-544`. На английской ОС
  (а `Caption` в разведке — английский) такой группы нет, правило не создавалось бы, а
  наследование к тому моменту уже снято — `default.vrd` остался бы без доступа вообще.
  Заодно: если учётки пула нет, наследование теперь не снимается совсем;
* у `<httpServices>` не пишется вообще никаких атрибутов — только вложенная запись `<service>`.
  Проверено на машине: с `pointEnableCommon` 1С отвечает 500 «Ошибка при разборе дескриптора
  виртуальных ресурсов. НачалоСвойства: pointEnableCommon Форма: Атрибут» — схема этот атрибут
  у `httpServices` не принимает (у `<ws>` он валиден, там его пишет сам `webinst`).
  `publishByDefault` из запасного `vrd` в `publish-tgbot.ps1` так же не проверен — не использовать,
  пока не понадобится и не подтвердится;
* пароль с `;` или кавычкой теперь отклоняется до правки файла — в строке соединения такой
  пароль рвётся, а наружу это выглядит как обычный 401;
* в лог дописывается копия `default.vrd` с замазанным паролем — его просят прислать в конце.

**Поймано на живой машине 07.08.2026:** после сужения прав 1С ответила 500 —
`Ошибка при разборе дескриптора виртуальных ресурсов ... default.vrd: 5(0x00000005): Отказано
в доступе`. Учётки пула в списке было мало: `wsisapi` читает `vrd` под токеном веб-запроса.
В список прав добавлены `IIS_IUSRS` (`S-1-5-32-568`) и `IUSR` (`S-1-5-17`). Лечение на месте,
без перезапуска скрипта:

```powershell
icacls C:\inetpub\tg1c\tgbot\default.vrd /grant "*S-1-5-32-568:(R)" "*S-1-5-17:(R)"
```

Заодно выяснилось, что ОС на машине русская, то есть замена `BUILTIN\Администраторы` на SID
здесь ничего не чинила — она про переносимость.

Итоговый адрес вебхука с учётом поправок:

```
https://tg.cvetkovtm.com:8443/tgbot/hs/tg/hook
```

### Состояние сайта после `setup-tgsite.ps1` (07.08.2026)

```
tg1c [Started] C:\inetpub\tg1c :: https *:8443:
Default Web Site [Started] ... :: http *:80: | https *:443:   (RDWeb, наружу не идёт)
```

* сертификат `CN=tg.cvetkovtm.com`, отпечаток `63CC26542562410C4B3ABE6D887726B1D929AE60`,
  действителен до 07.08.2036; публичная часть — `_tools/telegram-webhook.crt`, её грузить в `setWebhook`;
* `netsh http show sslcert ipport=0.0.0.0:8443`: `Negotiate Client Certificate: Disabled`,
  TLS 1.2 и 1.3 включены;
* `curl -k https://localhost:8443/` → **403** от IIS. Это правильный ответ на пустом сайте:
  листинг каталога выключен, публикации 1С там ещё нет.

**Не пугаться «Базовое соединение закрыто»** в старых версиях лога: `Invoke-WebRequest` под
PowerShell 5.1 запрещает пересогласование TLS и падает там, где всё исправно. Проверка в скрипте
переведена на `curl.exe`.

## Порядок

### 0. Убедиться, что конфигурация в базе свежая

```
git pull
```

Затем в Конфигураторе — «Загрузить конфигурацию из файлов» и обновление конфигурации БД.
`HTTPСервис.ТелеграмВебхук` и роль `ТелеграмВебхукДоступ` — новые объекты метаданных, поэтому
нужно принять структурные изменения, одного F7 без подтверждения мало.

### 1. Веб-сервер с CGI

PowerShell от администратора. Сначала определить тип ОС:

```powershell
(Get-CimInstance Win32_OperatingSystem).Caption
(Get-CimInstance Win32_OperatingSystem).ProductType   # 1 = клиентская, 2/3 = серверная
```

Серверная:

```powershell
Install-WindowsFeature -Name Web-Server,Web-CGI,Web-Mgmt-Console -IncludeManagementTools
```

Клиентская:

```powershell
Enable-WindowsOptionalFeature -Online -All -FeatureName `
  IIS-WebServerRole,IIS-WebServer,IIS-CommonHttpFeatures,IIS-CGI,IIS-ManagementConsole
```

`CGI` обязателен — без него модули 1С не работают.

Проверка:

```powershell
Get-Service W3SVC
Invoke-WebRequest http://localhost -UseBasicParsing | Select-Object StatusCode
```

**Перед этим шагом стоит выяснить, не опубликованы ли веб-сервисы МИКО (телефония,
`1C_MIKO_identify_number.1cws`) на ДРУГОЙ машине.** Если да — всю публикацию проще делать там:
веб-сервер и модули 1С уже настроены, останется одна галка.

### 2. Модули расширения веб-сервера 1С

Запустить `setup.exe` дистрибутива **той же версии, что установлена** → «Изменить» →
отметить «Модули расширения веб-сервера».

Версию посмотреть так:

```powershell
Get-ChildItem "C:\Program Files\1cv8" -Directory | Select-Object Name
```

Ожидается 8.3.25.1374 (версия из `Configuration.xml`).

Тихую установку через `msiexec ADDLOCAL=` **не использовать**: при неполном списке компонентов
она снесёт уже установленные, включая Конфигуратор.

Конфигуратор после установки перезапустить **от имени администратора** — иначе он не сможет
писать конфиг веб-сервера.

### 3. Пользователь 1С

Создать пользователя `ТелеграмВебхук`:

* аутентификация 1С:Предприятия — включить, задать длинный пароль (понадобится в `default.vrd`);
* аутентификация ОС и OpenID — снять;
* «Показывать в списке выбора» — снять;
* «Защита от опасных действий» — оставить;
* роль — **только** `ТелеграмВебхукДоступ`, никаких других.

Существующий пользователь «Служебный» не подходит: у него пустой пароль, а публикация будет
доступна из интернета.

### 4. Публикация

Конфигуратор → Администрирование → Публикация на веб-сервере. Отдельной публикацией
(алиас, например `tgbot`), а не поверх публикации МИКО.

* вкладка «HTTP-сервисы» — отметить `ТелеграмВебхук`;
* в `default.vrd` в строке соединения прописать `Usr=ТелеграмВебхук;Pwd=<пароль>`.

Про `Usr`/`Pwd` забыть нельзя: Telegram не умеет проходить аутентификацию 1С и получит 401.

Адрес получится такой:

```
https://<хост>/tgbot/hs/tg/hook
```

`hs` — фиксированный префикс HTTP-сервисов, `tg` — корневой URL сервиса, `/hook` — шаблон.

### 5. Проверка доступности — до всякого Telegram

С машины **вне локальной сети**:

```
curl -i -X POST https://<хост>/tgbot/hs/tg/hook -d "{}"
```

Правильный ответ на этом этапе — **403**. Он означает: сертификат принят, веб-сервер отдал
запрос в 1С, сервис отработал и отклонил его из-за пустого секрета. Это и есть критерий,
что публикация удалась.

Расшифровка других ответов:

| ответ | причина |
|---|---|
| 401 | не прописаны `Usr`/`Pwd` в `default.vrd` |
| 404 | сервис не отмечен в публикации, либо неверный алиас/путь |
| 500 | сервис вызвался, но упал — смотреть журнал регистрации, событие `Телеграм.Вебхук` |
| таймаут | база не видна из интернета: проброс порта, firewall, DNS |

Telegram принимает вебхук только на портах **443, 80, 88, 8443** и требует валидный сертификат
(самоподписанный тоже можно, но его нужно загружать в `setWebhook` — проще нормальный).

### 6. Секрет и включение

Придумать секрет: латиница, цифры, `_`, `-`, до 256 символов.

Вписать его в `СекретВебхука()` в `CommonModules/ТелеграмБот/Ext/Module.bsl` (сейчас там
`Возврат "";`), затем:

```
pwsh -NoProfile -File _tools/snapshot.ps1 "Telegram: включён вебхук"
```

Скрипт сам прогонит fix→check штрихкодов, закоммитит и запушит. Перед правкой — `git pull`,
на машине разработки файл могли поменять.

Загрузить конфигурацию из файлов, обновить конфигурацию БД. **Сразу после этого** вызвать
setWebhook (токен взять из `ТокенБота()`):

```
https://api.telegram.org/bot<ТОКЕН>/setWebhook
   ?url=https://<хост>/tgbot/hs/tg/hook
   &secret_token=<СЕКРЕТ>
   &allowed_updates=["callback_query","message"]
   &drop_pending_updates=true
```

**С учётом поправки 2** сертификат самоподписанный, поэтому это не GET по ссылке, а multipart-POST
с файлом `telegram-webhook.crt` (его кладёт рядом с собой `setup-tgsite.ps1`):

```powershell
$form = @{
  url                  = 'https://tg.cvetkovtm.com:8443/tgbot/hs/tg/hook'
  secret_token         = '<СЕКРЕТ>'
  allowed_updates      = '["callback_query","message"]'
  drop_pending_updates = 'true'
  certificate          = Get-Item '<путь>\telegram-webhook.crt'
}
Invoke-RestMethod -Uri "https://api.telegram.org/bot<ТОКЕН>/setWebhook" -Method Post -Form $form
```

`Invoke-RestMethod -Form` есть только в PowerShell 7 (`pwsh`). В 5.1 — тот же вызов через `curl.exe`:

```
curl.exe -F "url=https://tg.cvetkovtm.com:8443/tgbot/hs/tg/hook" ^
         -F "secret_token=<СЕКРЕТ>" ^
         -F "allowed_updates=[\"callback_query\",\"message\"]" ^
         -F "drop_pending_updates=true" ^
         -F "certificate=@<путь>\telegram-webhook.crt" ^
         https://api.telegram.org/bot<ТОКЕН>/setWebhook
```

Сертификат нужно перезагружать только при его перевыпуске. При смене одного лишь секрета
достаточно обычного GET-вызова без `certificate`.

`drop_pending_updates=true` выбросит накопившиеся необработанные нажатия — иначе они прилетят пачкой.

**Почему «сразу»:** заполненный секрет отключает опрос `getUpdates` (см. `ОбменСБотом`).
Между загрузкой конфигурации и вызовом setWebhook кнопки не работают вообще: опроса уже нет,
вебхука ещё нет. Делать эти два шага подряд, а не «секрет сегодня, setWebhook завтра».

### 7. Приёмка

В консоли кода на сервере:

```
Сообщить(ТелеграмБот.Диагностика());
```

Ожидается:

* `Вебхук: https://<хост>/tgbot/hs/tg/hook`
* `Нажатия кнопок: приносит вебхук (HTTPСервис.ТелеграмВебхук), опрос отключён`
* `Ждёт обновлений: 0`

Затем нажать в чате кнопку «🔄 <статус>» под любым сообщением: сообщение должно перерисоваться
**мгновенно**, всплывашка со сводкой — появиться сразу, а не через минуту.

В журнале регистрации на каждое нажатие должна появляться запись `Телеграм.Кнопка`.

## Чего НЕ делать

* Не трогать публикацию веб-сервисов МИКО — на ней работает телефония.
* Не давать пользователю `ТелеграмВебхук` никаких ролей, кроме `ТелеграмВебхукДоступ`,
  и тем более не «Полные права»: публикация смотрит в интернет.
* Не выключать и не удалять регламент `ТелеграмОбмен`. Вебхук закрывает только входящие
  нажатия. Очередь уведомлений о смене статуса и досылку отложенных с ночи уведомлений
  о новых заказах развозит он же — расписание раз в 1–2 минуты нужно при любом раскладе.
* Не включать вебхук, пока шаг 5 не дал 403 снаружи.

## Откат

Если что-то пошло не так:

1. `https://api.telegram.org/bot<ТОКЕН>/deleteWebhook`
2. вернуть в `СекретВебхука()` пустую строку, снимок, загрузить конфигурацию.

1С сразу вернётся к опросу регламентом — то есть к тому, как всё работает сейчас.

## Что прислать назад

* тип ОС и версию платформы;
* адрес публикации;
* ответ `curl` из шага 5;
* вывод `ТелеграмБот.Диагностика()` после включения;
* содержимое созданного `default.vrd` — на этой стороне его никто не видел, и полезно
  зафиксировать, как он выглядит для рабочей публикации HTTP-сервиса.
