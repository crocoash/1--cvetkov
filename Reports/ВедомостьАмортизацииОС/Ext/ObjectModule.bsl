#Если Клиент Тогда
// Настройка периода
Перем НП Экспорт;

////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ И ФУНКЦИИ 
// 

Процедура СформироватьОтчет(ДокументРезультат, ПоказыватьЗаголовок = Ложь, ВысотаЗаголовка = 0, ТолькоЗаголовок = Ложь) Экспорт

	// не выводим общие
	ОбщийОтчет.мСтруктураНевыводимыхГруппировок.Вставить("Общие", 1);

	ОбщийОтчет.ПостроительОтчета.Параметры.Вставить( "ДатаНач"              , ОбщийОтчет.ДатаНач);
	ОбщийОтчет.ПостроительОтчета.Параметры.Вставить( "ДатаКон"              , КонецДня(ОбщийОтчет.ДатаКон));
	ОбщийОтчет.ПостроительОтчета.Параметры.Вставить( "СостояниеВвода"       , Перечисления.СостоянияОС.ВведеноВЭксплуатацию);
	ОбщийОтчет.ПостроительОтчета.Параметры.Вставить( "СостояниеСнятияСУчета", Перечисления.СостоянияОС.СнятоСУчета);
	
	ОбщийОтчет.ВыводитьДополнительныеПоляВОтдельнойКолонке = Истина;
	
	ОбщийОтчет.СформироватьОтчет(ДокументРезультат, ПоказыватьЗаголовок, ВысотаЗаголовка, ТолькоЗаголовок);

КонецПроцедуры // СформироватьОтчет()

// Процедура - заполняет начальные настройки отчета
//
Процедура ЗаполнитьНачальныеНастройки() Экспорт
	
	ПостроительОтчета = ОбщийОтчет.ПостроительОтчета;
	
	Текст = 
	"ВЫБРАТЬ
	|	РегАмортизация.ОсновноеСредство                            КАК ОсновноеСредство,
	|	ПРЕДСТАВЛЕНИЕ(РегАмортизация.ОсновноеСредство)             КАК ОсновноеСредствоПредставление,
	|	РегСведенияОбОС.СпособНачисленияАмортизации                КАК СпособНачисления,
	|	ПРЕДСТАВЛЕНИЕ(РегСведенияОбОС.СпособНачисленияАмортизации) КАК СпособНачисленияПредставление,
	|	СУММА(РегАмортизация.СтоимостьНачальныйОстаток)            КАК СтоимостьНачальныйОстаток,
	|	СУММА(РегАмортизация.СтоимостьКонечныйОстаток)             КАК СтоимостьКонечныйОстаток,
	|	СУММА(РегАмортизация.СтоимостьОборот)                      КАК СтоимостьОборот,
	|	СУММА(РегАмортизация.АмортизацияНачальныйОстаток)          КАК АмортизацияНачальныйОстаток,
	|	СУММА(РегАмортизация.АмортизацияКонечныйОстаток)           КАК АмортизацияКонечныйОстаток,
	|	СУММА(РегАмортизация.АмортизацияОборот)                    КАК АмортизацияОборот,
	|	СУММА(РегАмортизация.СтоимостьКонечныйОстаток) 
	|		- СУММА(РегАмортизация.АмортизацияКонечныйОстаток)     КАК ОстаточнаяСтоимость,
	|	СУММА(РегСведенияОбОС.ПервоначальнаяСтоимость)             КАК ПервоначальнаяСтоимость,
	|	РегСостояниеОС.ДатаВводаВЭксплуатацию                      КАК ДатаВводаВЭксплуатацию,
	|	РегСостояниеОС.ДатаСнятияСУчета                            КАК ДатаСнятияСУчета,
	|	МестонахождениеОС.МОЛ                                      КАК МОЛ,
	|	ПРЕДСТАВЛЕНИЕ(МестонахождениеОС.МОЛ)                       КАК МОЛПредставление,
	|	МестонахождениеОС.Местонахождение                          КАК Местонахождение,
	|	ПРЕДСТАВЛЕНИЕ(МестонахождениеОС.Местонахождение)           КАК МестонахождениеПредставление
	|{ВЫБРАТЬ
	|	РегАмортизация.ОсновноеСредство.*           КАК ОсновноеСредство,
	|	РегСведенияОбОС.СпособНачисленияАмортизации КАК СпособНачисления,
	|	ДатаВводаВЭксплуатацию                      КАК ДатаВводаВЭксплуатацию,
	|	ДатаСнятияСУчета                            КАК ДатаСнятияСУчета,
	|	МОЛ,
	|	Местонахождение}
	|ИЗ
	|	РегистрНакопления.СтоимостьОС.ОстаткиИОбороты(&ДатаНач, &ДатаКон, , ,
	|	                  {ОсновноеСредство.* КАК ОсновноеСредство}) КАК РегАмортизация
	|		{ЛЕВОЕ СОЕДИНЕНИЕ 
	|			РегистрСведений.ПервоначальныеСведенияОС.СрезПоследних(&ДатаКон,
	|			                {ОсновноеСредство.* КАК ОсновноеСредство}) КАК РегСведенияОбОС
	|		ПО РегАмортизация.ОсновноеСредство = РегСведенияОбОС.ОсновноеСредство}
	|		{ЛЕВОЕ СОЕДИНЕНИЕ 
	|			(ВЫБРАТЬ
	|				СостоянияОС.ОсновноеСредство КАК ОсновноеСредство,
	|				МАКСИМУМ(ВЫБОР
	|						 	КОГДА СостоянияОС.Состояние = &СостояниеВвода
	|								ТОГДА СостоянияОС.ДатаСостояния
	|						 	ИНАЧЕ NULL
	|					     КОНЕЦ) КАК ДатаВводаВЭксплуатацию,
	|				МАКСИМУМ(ВЫБОР
	|						 	КОГДА СостоянияОС.Состояние = &СостояниеСнятияСУчета
	|								ТОГДА СостоянияОС.ДатаСостояния
	|						    ИНАЧЕ NULL
	|					     КОНЕЦ) КАК ДатаСнятияСУчета
	|			ИЗ
	|				РегистрСведений.СостоянияОС КАК СостоянияОС
	|			{ГДЕ
	|				СостоянияОС.ОсновноеСредство.*}
	|		
	|			СГРУППИРОВАТЬ ПО
	|				СостоянияОС.ОсновноеСредство) КАК РегСостояниеОС
	|		ПО РегАмортизация.ОсновноеСредство = РегСостояниеОС.ОсновноеСредство}
	|		{ЛЕВОЕ СОЕДИНЕНИЕ 
	|			РегистрСведений.МестонахождениеОС.СрезПоследних(
	|			                &ДатаКон,
	|			                {ОсновноеСредство.* КАК ОсновноеСредство}) КАК МестонахождениеОС
	|		ПО РегАмортизация.ОсновноеСредство = МестонахождениеОС.ОсновноеСредство}
	|{ГДЕ
	|	РегАмортизация.ОсновноеСредство.*,
	|	РегСведенияОбОС.СпособНачисленияАмортизации            КАК СпособНачисления,
	|	РегАмортизация.СтоимостьНачальныйОстаток,
	|	РегАмортизация.СтоимостьКонечныйОстаток,
	|	РегАмортизация.СтоимостьОборот,
	|	РегАмортизация.АмортизацияНачальныйОстаток,
	|	РегАмортизация.АмортизацияКонечныйОстаток,
	|	РегАмортизация.АмортизацияОборот,
	|	СУММА(РегАмортизация.СтоимостьКонечныйОстаток) 
	|		- СУММА(РегАмортизация.АмортизацияКонечныйОстаток) КАК ОстаточнаяСтоимость,
	|	РегСведенияОбОС.ПервоначальнаяСтоимость,
	|	РегСостояниеОС.ДатаВводаВЭксплуатацию,
	|	РегСостояниеОС.ДатаСнятияСУчета,
	|	МестонахождениеОС.МОЛ,
	|	МестонахождениеОС.Местонахождение}
	|
	|// Снятые с учета внутри периода не выводим
	|	ГДЕ ((РегСостояниеОС.ДатаСнятияСУчета Есть NULL) ИЛИ (РегСостояниеОС.ДатаСнятияСУчета >= &ДатаНач))
	|
	|СГРУППИРОВАТЬ ПО
	|	РегАмортизация.ОсновноеСредство,
	|	РегСведенияОбОС.СпособНачисленияАмортизации,
	|	РегАмортизация.ОсновноеСредство.Представление,
	|	РегСостояниеОС.ДатаВводаВЭксплуатацию,
	|	РегСостояниеОС.ДатаСнятияСУчета,
	|	МестонахождениеОС.МОЛ,
	|	МестонахождениеОС.Местонахождение
	|{УПОРЯДОЧИТЬ ПО
	|	ОсновноеСредство.*,
	|	ДатаВводаВЭксплуатацию,
	|	ДатаСнятияСУчета,
	|	СтоимостьНачальныйОстаток,
	|	СтоимостьКонечныйОстаток,
	|	СтоимостьОборот,
	|	АмортизацияНачальныйОстаток,
	|	АмортизацияКонечныйОстаток,
	|	АмортизацияОборот,
	|	ОстаточнаяСтоимость,
	|	СпособНачисления,
	|	ПервоначальнаяСтоимость,
	|	МОЛ,
	|	Местонахождение}
	|ИТОГИ
	|	СУММА(СтоимостьНачальныйОстаток),
	|	СУММА(СтоимостьКонечныйОстаток),
	|	СУММА(СтоимостьОборот),
	|	СУММА(АмортизацияНачальныйОстаток),
	|	СУММА(АмортизацияКонечныйОстаток),
	|	СУММА(АмортизацияОборот),
	|	СУММА(ОстаточнаяСтоимость),
	|	СУММА(ПервоначальнаяСтоимость)
	|ПО
	|	ОБЩИЕ,
	|	СпособНачисления
	|{ИТОГИ ПО
	|	ОсновноеСредство.*,
	|	СпособНачисления,
	|	МОЛ,
	|	Местонахождение}
	|АВТОУПОРЯДОЧИВАНИЕ";	
	СтруктураПредставлениеПолей = Новый Структура;
	СтруктураПредставлениеПолей.Вставить( "ОсновноеСредство",            "Основное средство");
	СтруктураПредставлениеПолей.Вставить( "ДатаВводаВЭксплуатацию",      "Дата ввода в эксплуатацию");
	СтруктураПредставлениеПолей.Вставить( "ДатаСнятияСУчета",            "Дата снятия с учета");
	СтруктураПредставлениеПолей.Вставить( "СпособНачисления",            "Способ начисления амортизации");
	
	СтруктураПредставлениеПолей.Вставить( "Местонахождение",             "Подразделение");
	
	СтруктураПредставлениеПолей.Вставить( "ПервоначальнаяСтоимость",     "Первоначальная стоимость");
	СтруктураПредставлениеПолей.Вставить( "СтоимостьНачальныйОстаток",   "Стоимость на начало периода");
	СтруктураПредставлениеПолей.Вставить( "СтоимостьОборот",             "Изменение стоимости");
	СтруктураПредставлениеПолей.Вставить( "СтоимостьКонечныйОстаток",    "Стоимость на конец периода");
	СтруктураПредставлениеПолей.Вставить( "ОстаточнаяСтоимость",         "Остаточная стоимость");
	
	СтруктураПредставлениеПолей.Вставить( "АмортизацияНачальныйОстаток", "Амортизация на начало периода");
	СтруктураПредставлениеПолей.Вставить( "АмортизацияОборот",           "Амортизация за период");
	СтруктураПредставлениеПолей.Вставить( "АмортизацияКонечныйОстаток",  "Амортизация на конец периода");
	
	ПостроительОтчета.Текст = Текст;
	
	УправлениеОтчетами.ЗаполнитьПредставленияПолей(СтруктураПредставлениеПолей, ПостроительОтчета);
	УправлениеОтчетами.ОчиститьДополнительныеПоляПостроителя(ПостроительОтчета);
	
	ОбщийОтчет.ЗаполнитьПоказатели( "ПервоначальнаяСтоимость",   "Первоначальная стоимость",    Истина, "ЧЦ=15; ЧДЦ=2");
	ОбщийОтчет.ЗаполнитьПоказатели( "СтоимостьНачальныйОстаток", "Стоимость на начало периода", Истина, "ЧЦ=15; ЧДЦ=2");
	ОбщийОтчет.ЗаполнитьПоказатели( "СтоимостьОборот",           "Изменение стоимости",         Истина, "ЧЦ=15; ЧДЦ=2");
	ОбщийОтчет.ЗаполнитьПоказатели( "СтоимостьКонечныйОстаток",  "Стоимость на конец периода",  Истина, "ЧЦ=15; ЧДЦ=2");
	ОбщийОтчет.ЗаполнитьПоказатели( "ОстаточнаяСтоимость",       "Остаточная стоимость",        Истина, "ЧЦ=15; ЧДЦ=2");
	
	ОбщийОтчет.ЗаполнитьПоказатели( "АмортизацияНачальныйОстаток", "Амортизация на начало периода", Истина, "ЧЦ=15; ЧДЦ=2");
	ОбщийОтчет.ЗаполнитьПоказатели( "АмортизацияОборот",           "Амортизация за период",         Истина, "ЧЦ=15; ЧДЦ=2");
	ОбщийОтчет.ЗаполнитьПоказатели( "АмортизацияКонечныйОстаток",  "Амортизация на конец периода",  Истина, "ЧЦ=15; ЧДЦ=2");
	
	МассивОтбора = Новый Массив;
	МассивОтбора.Добавить("ОсновноеСредство");
	
	УправлениеОтчетами.ЗаполнитьОтбор(МассивОтбора, ПостроительОтчета);

	ОбщийОтчет.ВыводитьПоказателиВСтроку = Истина;
	ОбщийОтчет.РаскрашиватьИзмерения     = Истина;
	
КонецПроцедуры // ЗаполнитьНачальныеНастройки()

// Читает свойство Построитель отчета
//
// Параметры
//	Нет
//
Функция ПолучитьПостроительОтчета() Экспорт

	Возврат ОбщийОтчет.ПолучитьПостроительОтчета();

КонецФункции // ПолучитьПостроительОтчета()

// Настраивает отчет по переданной структуре параметров
//
// Параметры:
//	Нет.
//
Процедура Настроить(Параметры) Экспорт

	ОбщийОтчет.Настроить(Параметры, ЭтотОбъект);

КонецПроцедуры // Настроить()

// Возвращает основную форму отчета, связанную с данным экземпляром отчета
//
// Параметры
//	Нет
//
Функция ПолучитьОсновнуюФорму() Экспорт
	
	ОснФорма = ПолучитьФорму();
	ОснФорма.ОбщийОтчет = ОбщийОтчет;
	ОснФорма.ЭтотОтчет  = ЭтотОбъект;
	
	Возврат ОснФорма;
	
КонецФункции // ПолучитьОсновнуюФорму()

// Возвращает форму настройки 
//
// Параметры:
//	Нет.
//
// Возвращаемое значение:
//	
//
Функция ПолучитьФормуНастройки() Экспорт
	
	ФормаНастройки = ОбщийОтчет.ПолучитьФорму("ФормаНастройка");
	Возврат ФормаНастройки;
	
КонецФункции // ПолучитьФормуНастройки()

// Процедура обработки расшифровки
//
// Параметры:
//	Нет.
//
Процедура ОбработкаРасшифровки(РасшифровкаСтроки, ПолеТД, ВысотаЗаголовка, СтандартнаяОбработка) Экспорт
	
	// Добавление расшифровки из колонки
	Если ТипЗнч(РасшифровкаСтроки) = Тип("Структура") Тогда
		
		// Расшифровка колонки находится в заголовке колонки
		РасшифровкаКолонки = ПолеТД.Область(ВысотаЗаголовка+2, ПолеТД.ТекущаяОбласть.Лево).Расшифровка;

		Расшифровка = Новый Структура;

		Для каждого Элемент Из РасшифровкаСтроки Цикл
			Расшифровка.Вставить(Элемент.Ключ, Элемент.Значение);
		КонецЦикла;

		Если ТипЗнч(РасшифровкаКолонки) = Тип("Структура") Тогда

			Для каждого Элемент Из РасшифровкаКолонки Цикл
				Расшифровка.Вставить(Элемент.Ключ, Элемент.Значение);
			КонецЦикла;

		КонецЕсли; 

		ОбщийОтчет.ОбработкаРасшифровкиСтандартногоОтчета(Расшифровка, СтандартнаяОбработка, ЭтотОбъект);

	КонецЕсли;
	
КонецПроцедуры // ОбработкаРасшифровки()

// Формирует структуру, в которую складываются настройки
//
Функция СформироватьСтруктуруДляСохраненияНастроек(ПоказыватьЗаголовок) Экспорт
	
	СтруктураНастроек = Новый Структура;
	
	ОбщийОтчет.СформироватьСтруктуруДляСохраненияНастроек(СтруктураНастроек, ПоказыватьЗаголовок);
	
	Возврат СтруктураНастроек;
	
КонецФункции

// Заполняет настройки из структуры - кроме состояния панели "Отбор"
//
Процедура ВосстановитьНастройкиИзСтруктуры(СохраненныеНастройки, ПоказыватьЗаголовок, Отчет=Неопределено) Экспорт
	
	// Если отчет, вызвавший процедуру, не передан, то считаем, что ее вызвал этот отчет
	Если Отчет = Неопределено Тогда
		Отчет = ЭтотОбъект;
	КонецЕсли;

	ОбщийОтчет.ВосстановитьНастройкиИзСтруктуры(СохраненныеНастройки, ПоказыватьЗаголовок, Отчет);
	
КонецПроцедуры

ОбщийОтчет.ИмяРегистра = "СтоимостьОС";
ОбщийОтчет.мНазваниеОтчета = "Ведомость по амортизации ОС";
ОбщийОтчет.мВыбиратьИмяРегистра = Ложь;
ОбщийОтчет.мРежимВводаПериода = 0;
ОбщийОтчет.мВыбиратьИспользованиеСвойств = Ложь;
#КонецЕсли
